let Power =
    -- Base unit: power.
      { microwatts : Double }

let Time =
    -- Base unit: time.
      { microseconds : Double }

let Frequency =
    --Base unit: frequency.
      { hertz : Double }

in  { Frequency = Frequency, Time = Time, Power = Power }
